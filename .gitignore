# OS artifacts
Thumbs.db
Desktop.ini
.DS_Store

# Editors / IDEs
.idea/
.vscode/
*.iml

# Local scratch
*.log
*.tmp
node_modules/

# Local planning docs (not for the website repo)
platform-docs/

# Platform build artifacts (D-12: generated locally, never committed)
dist/
.next/
coverage/
*.tsbuildinfo

# Platform runtime secrets (generated on first boot by scripts/init, never committed)
secrets/
.env

# Platform backups (PC-11: DB dumps/WAL segments contain real data, never committed)
backups/

# Scratch dir used for git-lock/debris recovery on this machine (never committed)
_delete_me/
