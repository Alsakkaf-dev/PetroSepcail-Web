// Contract-drift check (ADR-13): verifies packages/contracts' zod schemas
// stay in sync with 05-api-specification.md. No schemas exist yet — the
// first land with PC-01/02 in S02 — so this is a no-op pass until then.
console.log("[test:contract] no contracts defined yet (first land in S02) — pass");
